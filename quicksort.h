#define NR_SUBJECTS 16
#define NR_SCORE 10
void quickSort(unsigned int arr[], unsigned int indexes[], int low, int high);
void sortByIndex(unsigned int values[][NR_SUBJECTS], unsigned int indexes[]);
